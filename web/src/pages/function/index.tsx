import React from "react";
import {PageContainer} from "@ant-design/pro-components";

const LocalService: React.FC = () => {
  return <PageContainer
    title={"函数管理"}
    content={"展示和操作远端部署的函数服务"}
    extra={""} />
}

export default LocalService
